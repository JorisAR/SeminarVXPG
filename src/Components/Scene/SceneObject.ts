import p5 from 'p5';

export class SceneObject {
    //should have a render function
    //should have a geometry function
    //lets just use inheritence for this:
    //create rectangle, chair, table, vase
}